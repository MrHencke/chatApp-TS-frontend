const HTTP = 'http://';
const HTTPS = 'https://';
const BACKENDURL = 'no-hencke-chatapp-backend.herokuapp.com';
const BACKEND_PORT = '6155';
const APIROUTE = '/api/';
const SOCKETROUTE = '/socket';

const config = {
	HTTP,
	HTTPS,
	BACKENDURL,
	BACKEND_PORT,
	APIROUTE,
	SOCKETROUTE,
};
export default config;
