const HTTP = 'http://';
const HTTPS = 'https://';
const BACKENDURL = 'localhost';
const BACKEND_PORT = '8002';
const APIROUTE = '/api/';
const SOCKETROUTE = '/socket';

const config = {
	HTTP,
	HTTPS,
	BACKENDURL,
	BACKEND_PORT,
	APIROUTE,
	SOCKETROUTE
};
export default config;
